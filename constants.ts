export enum ROLE {
  ADMIN = "ADMIN",
  PLAYER = "PLAYER",
  QUEUED = "QUEUED"
}

export const JWT_COOKIE_NAME = "jwt";

export enum REGISTRATION_STATUS {
  OPEN = "OPEN",
  FULL = "FULL"
}

export const MAX_PLAYERS = 12
